from numpy import array
from types import Tuple

def detect_plate(img: array) -> Tuple[array, array]:
    roi = array()
    coord = array()
    return roi, coord

def get_text(img: array) -> str:
    pass
